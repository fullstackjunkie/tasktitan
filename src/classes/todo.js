class Todo {
    
    constructor(title, descr, dueDate, priority) {
        this.title = title;
        this.description = descr;
        this.due = dueDate;
        this.priorityLevel = priority;
    }

}